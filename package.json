{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "lerna run prepare",
    "problems": "lerna run problems",
    "build": "lerna run build",
    "build-tests": "lerna build",
    "pretest": "lerna run problems",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "*"
  },
  "resolutions": {
    "typescript": "~2.8.4",
    "@types/qunit": "^2.0.31",
    "ember-cli": "^3.1.4",
    "lerna": "^2.11.0",
    "libkit": "^0.6.0",
    "no-show": "^0.6.2",
    "prettier": "^1.12.1",
    "qunit": "^2.4.1",
    "shelljs": "^0.7.8",
    "ts-std": "^0.6.2",
    "tslint": "^5.7.0"
  }
}
